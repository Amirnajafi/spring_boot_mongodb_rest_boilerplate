version: '3.3'
services:
  api:
    image: springboot
    container_name: springboot
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - /data/springboot
    ports:
      - "8081:8081"
    networks:
      - default
networks:
  default:
    external:
      name: docker